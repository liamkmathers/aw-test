# Working notes: T-002

Written by hand. `notes-export.sh` produced an empty file because no `note`
events reached `~/.claude/status/*.jsonl` — see the last entry below.

- The 0.1.2 `CLAUDE.workflow.md` template file itself ends with the `## Things Claude gets wrong` heading and its parenthetical line. No conflict with the instruction to leave that section alone, because the repo's existing line is already verbatim what the template carries, so a section-to-EOF diff satisfies "matches exactly" without changing it.
- The 0.1.2 `pr.yml` template puts `shell: bash` on the typecheck step. On `ubuntu-latest` that is a no-op (default is already `bash -e`) and the acceptance criterion does not ask for it. Included to match the template; Liam confirmed.
- New 0.1.2 workflow rule 6 — "Edit repo files with the Write and Edit tools, never with shell redirection, `sed -i`, or scripts. Hooks only see tool edits." `tasks/T-002.md` was created with a heredoc before that template had been read, so no hook saw it. Switched to Write/Edit from that point on.
- The plugin's own `PostToolUse` hook `scripts/format-changed.sh` runs `prettier --write` on every `.md` file edited with the Write/Edit tools, which rule 6 requires — but the templates the plugin ships are not prettier-clean. Running prettier on the fetched template itself changes it. Byte-exact template matching is therefore impossible while the plugin is installed. Stopped under rule 10; Liam chose `.prettierignore`.
- `tasks/INDEX.md` is generated by the plugin's `task-index.sh` hook. It appeared untracked when `tasks/T-002.md` was created and must list T-002 to be truthful, so it was committed even though it is not in the expected-files list.
- `REVIEW.md` tells the compliance reviewer to check diffs against `docs/NORTH_STAR.md`, which does not exist in this repo. Pre-existing, out of scope for T-002, but it will misfire on every review until someone writes it.
- `agents/verifier.md` at 0.1.2 hardcoded `npm test` and `npx tsc --noEmit`. That bare `tsc` is the exact command T-002 fixes, so the verifier would have reported a false `Typecheck: FAIL` on a correct tree. `stop-gate.sh` got this right by reading `.agent-workflow`. Fixed upstream in 0.1.3.
- Two verifier runs under 0.1.2 stalled at the 600s watchdog without writing `tasks/T-002.verify.md`. Not caused by the repo — every gate command completes in under a second when run directly.
- The stop gate fired on a message that contained the completion phrase in backticks while explaining the gate. It greps the transcript and cannot distinguish a declaration from a mention. Addressed by 0.1.3's new item 12.
- 0.1.3 rewrote item 12 of `CLAUDE.workflow.md`. Criterion 3 names the raw URL on `main` rather than a pinned tag, so that URL now serves 0.1.3 text: `CLAUDE.md` here matches 0.1.2, which is what the task targets, but no longer matches what the URL serves today. The verifier judged the 0.1.2 reading correct and marked the criterion PASS.
- `notes-export.sh` builds this file from `note` events in `~/.claude/status/*.jsonl`, but that feed holds only `start`, `stop`, and `tool` events — no `note` events exist in any session on this machine. `status-emit.sh` scans for `NOTE:` lines only on `PostToolUse`, against a per-session offset file. Rule 5's promise that hooks collect these is not currently holding.
