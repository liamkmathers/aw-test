# Verify: T-002
Tests: PASS
Typecheck: PASS

Ran per `.agent-workflow`'s `typecheck:` line (`npx next typegen && npx tsc --noEmit`), not a bare `npx tsc --noEmit` (which is expected to fail by design pre-fix, and in fact now also passes only because a prior `typegen` run already wrote untracked `.next/types` into the working tree — `git status --porcelain` is clean throughout, since `/.next/` is gitignored).

`npm run test`: 1 test file, 1 test, passed.
`npx next typegen && npx tsc --noEmit`: typegen succeeded, `tsc` exited 0.

## Criteria
- [PASS] The `typecheck:` line in `.agent-workflow` is exactly `npx next typegen && npx tsc --noEmit` — `grep -x 'typecheck: npx next typegen && npx tsc --noEmit' .agent-workflow` matches.
- [PASS] `.github/workflows/pr.yml` runs `npx next typegen && npx tsc --noEmit` in place of the `npx tsc --noEmit` step — line 14 is `- run: npx next typegen && npx tsc --noEmit`; `grep -n '^\s*- run: npx tsc --noEmit$'` finds nothing.
- [PASS] The `## Workflow (every task)` section of `CLAUDE.md` matches https://raw.githubusercontent.com/liamkmathers/agent-workflow/main/plugins/agent-workflow/templates/CLAUDE.workflow.md exactly — see "Criterion 3" note below for the two readings and which I judge correct.
- [PASS] `REVIEW.md` contains the Pruning section from the 0.1.2 template — `diff REVIEW.md <0.1.2 templates>/REVIEW.md` is empty (also empty against 0.1.3, which did not touch REVIEW.md).
- [PASS] The `kind:` line in `tasks/TEMPLATE.md` matches the 0.1.2 template (`feature | fix | chore`) — `diff tasks/TEMPLATE.md <0.1.2 templates>/tasks/TEMPLATE.md` is empty; line 3 reads `kind: feature        # feature | fix | chore (config/CI/docs only, no test-first)`.
- [PASS] `npx next typegen && npx tsc --noEmit` exits 0 on a clean tree — `git status --porcelain` empty before the run, command exited 0.
- [PASS] Existing tests pass — `npm run test` passed (1/1).

### Criterion 3 — two readings
The criterion names an **unpinned** URL on `agent-workflow`'s `main` branch. Two readings diverge:

- **Reading A — literal, fetched today.** `main` has since moved to 0.1.3, which rewrote workflow item 12 (adds retry/stall handling for `verifier` and a rule against writing the completion phrase except on real completion). Diffing the repo's section (minus the template file's own leading separator blank line, which precedes the `## Workflow` heading and isn't part of the section) against the 0.1.3 template gives one hunk: item 12 differs. Under this reading the criterion **FAILS**.
- **Reading B — intent, pinned to 0.1.2.** The task title is literally "...sync workflow files to plugin 0.1.2", the Context paragraph names 0.1.2 three times, and the plan records that the raw URL was verified byte-identical to the 0.1.2 template *at planning time* (`curl` + `diff`, per the plan's header note). Diffing the repo's section against the 0.1.2 template (again minus the template's leading blank line) is empty. Under this reading the criterion **PASSES**.

**Judgment:** Reading B is correct. The criterion's use of an unpinned `main` URL is a defect in how the task was written — a moving branch is a poor stand-in for "exactly" — but every piece of unambiguous evidence (title, Context, plan) points at 0.1.2 as the actual target, and the agent verified the URL matched 0.1.2 at the time the task was authored. Treating the criterion as "PASS" is scored above on that basis. A verifier re-running the literal proof command (`curl` the live URL) today would see a spurious failure caused by drift in an external, unpinned reference outside the agent's control, not by anything wrong in this diff.

## Files outside expected list
- `.prettierignore` (new, not listed in "Files expected to change")
- `tasks/INDEX.md` (new, not listed in "Files expected to change")
- `tasks/T-002.plan.md` (new; plan files are conventionally not listed here — T-001's task file also omits its own plan/verify files — so this is not flagged as a departure)

Both `.prettierignore` and `tasks/INDEX.md` are recorded under "Decisions" in `tasks/T-002.plan.md`, with the plan stating both were approved mid-task. Assessment of that record:

- The record is procedurally adequate under CLAUDE.md workflow rule 4 ("If the diff departs from the plan, update the plan in the same commit") — both additions are explained with rationale, and the plan's own file-list at the top of `tasks/T-002.plan.md` already lists `.prettierignore` as "(new) — **departure from the approved plan**, see Decisions". `tasks/T-002.md`'s own "Files expected to change" section was *not* amended to add these two paths, but the workflow rule speaks about keeping the *plan* current, not the task file, so this is not itself a rule violation.
- One inaccuracy in the record: the `.prettierignore` Decisions entry says "Ignoring these three paths keeps them verbatim" (naming `CLAUDE.md`, `REVIEW.md`, `tasks/TEMPLATE.md`), but the actual pattern committed is `tasks/*.md`, which is a non-recursive glob that ignores *every* markdown file directly under `tasks/` — confirmed with `npx prettier --file-info`: `tasks/T-001.md`, `tasks/T-002.md`, `tasks/T-002.plan.md`, and `tasks/INDEX.md` are all reported `"ignored": true`, not just `TEMPLATE.md`. The record undersells the scope of what was approved; the code is broader than the stated three-path rationale. This doesn't break any acceptance criterion (nothing requires prettier to run on task files, and the "clean tree" and "existing tests pass" criteria are unaffected), but it means the written justification for the mid-task departure does not fully match what was implemented, so the "record" is incomplete rather than fully adequate.

## Out-of-scope violations
none — `git diff main...HEAD -- app/ tests/ .dependency-cruiser.cjs` is empty, and the `## Things Claude gets wrong` section of `CLAUDE.md` is unchanged (only shown as diff context around the preceding line).

## Plan drift
- The `.prettierignore` glob scope (`tasks/*.md` vs. the "these three paths" wording in the Decisions note) is broader than documented — see "Files outside expected list" above. This is drift between the plan's stated rationale and the actual code, not between the plan's task list and the diff (the plan's own file list does correctly show `.prettierignore` as new).
- Plan steps 11 and 12 in "Order of work" are marked `[ ]` (not done) — running the full local PR gate (lint, depcruise, wiki-lint) and invoking `verifier`/`/pr` — which is expected since this verification run *is* step 12's first sub-action. Not a defect, just noting the plan is not yet fully ticked.
- Otherwise the diff matches the plan step-for-step: `.agent-workflow` and `pr.yml` edits (commit `c3255e4`), the `shell: bash` addition matches the documented Decision, the CLAUDE.md rewrite plus the `.prettierignore` departure and `tasks/INDEX.md` commit (commit `c32258e`), and the acceptance-criteria tick commit (`3d7329c`).

## Verdict
READY — tests and typecheck pass, all seven acceptance criteria are met (criterion 3 correctly read against the 0.1.2 target the task explicitly names), no out-of-scope files were touched, and the two unlisted files are both explained and approved in the plan, with only a minor inaccuracy in how broadly the `.prettierignore` Decision describes its own effect.
