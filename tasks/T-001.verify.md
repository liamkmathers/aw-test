# Verify: T-001

Tests: PASS
Typecheck: PASS

## Tests / Typecheck detail

- `npm run test` -> 2 test files, 3 tests, all passed (`tests/smoke.test.ts`, `tests/about.test.tsx`).
- `npx tsc --noEmit` (in the worktree as-is, with `.next/` present from the earlier `npm run build`) -> exits 0, no output.
- Independently reproduced the pre-existing `LayoutProps` failure claimed in the plan: copied the
  worktree to a scratch dir excluding `.next/`, ran `npx tsc --noEmit` there -> fails with
  `app/layout.tsx(20,50): error TS2304: Cannot find name 'LayoutProps'`. Then also removed
  `app/about/` from that same copy and re-ran -> the `LayoutProps` error persists identically
  (plus a new, expected `Cannot find module '../app/about/page'` error from the test file, which
  is a side effect of deleting the page, not evidence against the claim). Also confirmed
  `app/layout.tsx` on `main` itself already references `LayoutProps<"/">`. This confirms the
  failure is pre-existing, caused by `.next/types/routes.d.ts` (which defines `LayoutProps` and
  is only generated by `next build`/`next dev`/`next typegen`) not existing yet, and is unrelated
  to T-001. Claim confirmed independently, not taken on word.
- `npm run lint` -> clean, no output/errors.
- `npx depcruise --config .dependency-cruiser.cjs app` -> "no dependency violations found".
- `bash scripts/wiki-lint.sh` -> clean, no output.

## Criteria

- [PASS] GET /about returns 200 — Ran `npm run build` (succeeds, route table lists `○ /about` as
  static) then `npm start -p 3211` and `curl -o /dev/null -w '%{http_code}' http://localhost:3211/about`,
  got `200`. Response body contains `<h1>About</h1>`. Control request to `/nope` returned `404`,
  confirming the 200 is specific to the route and not a catch-all. This matches the approved
  approach: the committed test (`tests/about.test.tsx`) only asserts this structurally (route file
  exists + component renders without throwing), and the literal HTTP 200 is proven by hand per the
  plan; I reproduced that proof myself rather than trusting the plan's record of it.
- [PASS] The page renders an h1 with the exact text "About" — `app/about/page.tsx` returns
  `<h1>About</h1>` exactly; `tests/about.test.tsx` asserts `renderToStaticMarkup(<Page />)` equals
  the literal string `"<h1>About</h1>"`; `npm run test` passes this assertion.
- [PASS] A test in tests/ covers both of the above — `tests/about.test.tsx` contains both the
  structural "GET /about returns 200" test and the exact-heading test.
- [PASS] Existing tests pass — `tests/smoke.test.ts` still passes alongside the new test file
  (`npm run test` reports 2 files / 3 tests, all green).

## Mutation check (independent, not just trusting the plan's NOTE)

Ran the test file against three broken implementations in a scratch copy of the repo:

- `<h1>About us</h1>` -> exact-heading test fails as expected (`AssertionError: expected
'<h1>About us</h1>' to be '<h1>About</h1>'`); the structural test still passes (as designed,
  since it only checks file existence + non-throwing render).
- Deleting `app/about/` entirely -> the whole test file fails at import time
  (`Cannot find module '../app/about/page'`), so both the "route exists" intent and the render
  intent are guarded, just via an import-time failure rather than a per-test assertion failure.
- (Not independently re-tested, but consistent with the above): a wrapping `<div>` would fail the
  same exact-string equality check the "About us" mutant failed.
  This confirms the test is not vacuous and would fail against a wrong implementation, within the
  accepted structural-proxy trade-off for the "200" criterion (a route file could theoretically be
  misconfigured such that Next still returns non-200 for other reasons — e.g. a thrown error during
  static generation — that this test would not catch pre-build; the hand-verified curl closes that
  gap once, as documented).

## Files outside expected list

None. `git diff main...HEAD --stat` shows exactly three files: `app/about/page.tsx`,
`tests/about.test.tsx`, `tasks/T-001.plan.md` — matching the plan's "Files that change" list
exactly (the task file's own "Files expected to change" list omits the plan file itself, but the
plan document explicitly says the task's list is binding for the _implementation_ and the plan is
naturally added alongside it; this was already reconciled in the plan text).

Note: an untracked, uncommitted `tasks/T-001.log.md` exists in the working tree (hook-generated
NOTE log per the workflow in CLAUDE.md). It is not part of any commit on this branch and does not
show up in `git diff main...HEAD`, so it is not a files-changed violation, but flagging it for
visibility.

## Out-of-scope violations

None. `app/about/page.tsx` has no styling (no className, no imports beyond none needed) beyond
whatever the root layout already applies by default. `app/page.tsx` (the home page) is byte-for-
byte unchanged on this branch — no nav link was added.

## Plan drift

None. Every step in `tasks/T-001.plan.md`'s "Order of work" is reflected in the diff and commit
history (test-first commit `bb0e70b`, implementation commit `eea0da0`, evidence-recording commit
`e4f6a86`, plan commit `787abc9`), and there is no code in the diff that isn't accounted for by the
plan (just the two small files plus the plan document itself).

## .claude/rules/ui.md

Rules apply to `app/**/*.tsx`. `app/about/page.tsx` is a single `<h1>About</h1>` with no
`<select>`/`<input type=date>`/browser dialogs, no hardcoded font-family, no motion, no loading
spinner, and no citations/sources — every rule is vacuously satisfied by the page's minimalism.

## Verdict

READY — all acceptance criteria hold under independent re-verification (including the pre-existing
`LayoutProps` claim and the hand-proven HTTP 200), the diff is exactly the three files the plan
commits to, nothing out of scope was touched, and the test fails against real mutations.
